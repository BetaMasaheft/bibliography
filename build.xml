<?xml version="1.0" encoding="UTF-8"?>
<project default="xar" name="EthioStudies">
    <xmlproperty file="expath-pkg.xml" />
    <property name="project.version" value="${package(version)}" />
    <property name="project.app" value="EthioStudies" />
    <property name="build.dir" value="build" />
    <!--
      expand.xqm: collection("/db/apps/EthioStudies")//t:biblStruct by tag.
      string:Zotero(): citations.xml @tag = bm:… / xhtml csl-entry.
      CSL and GitHub Pages stay in git, not in the xar.
    -->
    <target name="clean" description="delete all build artefacts">
        <delete includeemptydirs="true" failonerror="false">
            <fileset dir="${build.dir}" includes="**/*" />
        </delete>
    </target>
    <target name="xar" depends="clean">
        <mkdir dir="${build.dir}" />
        <zip destfile="${build.dir}/${project.app}-${project.version}.xar">
            <fileset dir="." includes="expath-pkg.xml,repo.xml,EthioStudies.xml,citations.xml" />
        </zip>
    </target>
</project>
